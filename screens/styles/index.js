import {
    StyleSheet,
  } from 'react-native';

  const styles = StyleSheet.create({
    flex: {
      display: 'flex',
      flexDirection: 'row',
      width: '100%',
      alignItems: 'center',
      marginTop: 16,
    },
    sectionContainer: {
      marginTop: 32,
      paddingHorizontal: 24,
    },
    sectionTitle: {
      fontSize: 24,
      fontWeight: '600',
    },
  });

  export default styles;